# -*- coding: utf-8 -*-
"""
listen_health —— 动态监听健康插件。

治的是这个偶发故障：wxautox `AddListenChat` 双击会话后没弹出独立聊天窗口，
拿到 0 句柄，`win32gui.MoveWindow` 抛 `error(1400, 'MoveWindow', '无效的窗口句柄。')`。
命中时该会话开不出子窗口，走主窗口回落（见 wxbot_core.MainWindowChat）。

2026-08-04 排查现状：**根因未知**。已被证据推翻的三个假说，别再走回头路——
1. 「微信独立窗口有 5 个上限」——手动开到 8 个照样能开第 9 个，且 08-04 00:49/08:04
   在 5 个窗口占满时两次成功。
2. 「朋友圈点赞把 UI 搞脏」——只对上 08-04 13:46 那次（相隔 52 秒），
   08-03 20:34 那次距上次点赞 2h43m。专门跑了 6 轮「开关朋友圈后立刻调用」，6/6 成功。
3. 「wxautox/UIA 连接随运行时长老化」——成功样本覆盖 0~49.84 小时，
   失败样本 1.15/1.20/13.44 小时，完全重叠。

★ 2026-08-04 wxautox 作者 Siver 的回复校正了性质判断：这是**微信窗口丢失**，
「碰到了重启就好了」，可能是微信/Windows 自身的问题，也可能是人为或后台软件干扰。
回头看日志完全对得上——它不是随机抖动，而是**进坏状态后持续失败**：
08-03 20:34 失败、20:37 再失败（其间 21:24 朋友圈点赞正常，所以坏的只是
「开独立窗口」这一个能力，不是整个 UIA）；08-04 13:46 失败。两次都在重启程序后
立刻全好，**而微信客户端全程没重启过**。所以自愈不需要人扫码，可以全自动。
（也因此，我此前「5% 独立随机」的判断是错的：连续两次失败在独立随机下只有 0.25%
的概率；那 18 轮全过是因为测在重启之后，测的是健康状态。）

本插件不试图修根因，只做三件事：

- alert.py：最终失败时推告警（webhook + 管理群），别再靠人肉翻日志发现。
- probe.py：定时探针 + 环境快照，攒失败样本，等样本够了再回来找规律。
- heal.py：探针确认坏状态后自动重启进程；重启完还坏才叫人（那才轮到重启微信）。

两者都挂在 bot 进程内、由 schedule 主循环串行调用 —— 微信 UI 操作绝不能另起
进程或线程，会和主循环抢窗口（见 CLAUDE.md 3.11 的血泪）。
"""
from .alert import alert_listen_failure
from .probe import register

__all__ = ['alert_listen_failure', 'register']
